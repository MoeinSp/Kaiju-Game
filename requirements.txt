# [webhooks] pulls in the tornado-based web server PTB needs for run_webhook();
# without the extra the bot works in polling mode but crashes in webhook mode.
python-telegram-bot[webhooks]>=22.0
Django>=5.0
python-dotenv>=1.0
# panel: static files without a separate nginx location, and a WSGI server
whitenoise>=6.6
gunicorn>=22.0
# Postgres driver (binary wheel — no libpq build step in the image)
psycopg[binary]>=3.1
