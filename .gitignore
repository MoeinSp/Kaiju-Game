.venv/
venv/
__pycache__/
*.pyc
.env
# Deployment-local compose overrides (host ports, proxy wiring) — never committed
docker-compose.override.yml
*.db
*.sqlite3
.idea/
staticfiles/
backups/

# One-off VPS/ops fix scripts (not app code)
scripts/
assets/images/cache/
previews/
test_*.png

