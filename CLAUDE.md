# Kaiju Bio-Lab — Telegram Chat Game

بات بازی تلگرام: رشد و ترکیب ژنتیکی هیولا در پیوی، دوئل/رید باس در گروه.

## ساختار پروژه (modular monolith)

- `bot/` — لایه تلگرام. فقط parse کردن آپدیت و صدا زدن `game/` و `db/`. منطق بازی اینجا نباید باشه.
  - `bot/handlers/private.py` — دستورات پیوی (`/start`, `/me`, `/collection`, `/select`, `/splice`, `/missions`) + دکمه‌های شیشه‌ای آزمایشگاه.
  - `bot/handlers/group.py` — دستورات گروه (`/duel`, `/raid_spawn`, `/attack`, `/leaderboard`, `/guardian`, `/guardian_challenge`, `/guardian_claim`, `/give`, `/mutation_event`).
  - `bot/handlers/battle.py` — نبرد تعاملی/اسکیل‌دار (`/battle`) با دکمه‌های شیشه‌ای نوبت‌به‌نوبت.
  - `bot/handlers/misc.py` — `/help` (کار می‌کنه هم توی پیوی هم گروه).
- `game/` — منطق خالص بازی، بدون وابستگی به تلگرام. قابل تست مستقل.
  - `constants.py` — عناصر، اجزای بدن، rarity، اسکیل‌ها، هزینه‌ها، فرمول‌ها.
  - `creature.py` — ساخت موجود، استت مؤثر، تغذیه/تمرین/ارتقا، لول‌آپ، کلکسیون، جهش تصادفی.
  - `combat.py` — دوئل خودکار (نوبتی، بر اساس SPD/ATK/DEF/عنصر) + لاگ متنی. برای وقتی حریف آفلاینه یا حوصله‌ی تعامل نیست.
  - `interactive_battle.py` — نبرد تعاملی زنده با اسکیل مخصوص هر عنصر (آتش=دمیج مضاعف، آب=شفا، خاک=سپر، برق=استان). state روی `InteractiveBattle` نگه داشته می‌شه چون بین چند callback پخش می‌شه.
  - `raid.py` — اسپان باس، حمله با کول‌داون، تقسیم غنایم بر اساس سهم دمیج.
  - `splice.py` — ترکیب DNA دو موجود.
  - `daily.py` — ماموریت روزانه، انرژی/استامینا روزانه، رویداد گروهی یک‌بار-در-روز. `ENERGY_CAPS`/`assert_energy_available` یه الگوی عمومیه — هر اکشن جدیدی که باید سقف روزانه داشته باشه (نه فقط feed/raid_attack) فقط با یه کلید جدید تو دیکشنری اضافه می‌شه.
  - `guardian.py` — محافظ گروه پایدار (روی `Group.guardian_creature_id`)، نه فقط نمایشی: با `/guardian_challenge` واقعاً می‌شه عنوان رو با دوئل گرفت.
  - `trade.py` — هدیه دادن مالکیت یه موجود به بازیکن دیگه.
- `db/` — مدل‌های SQLAlchemy (`models.py`)، session/engine (`session.py`)، کوئری‌های مشترک از جمله عضویت گروهی (`repository.py`).

نکته زیرساختی: SQLite با `PRAGMA journal_mode=WAL` + `busy_timeout` تنظیم شده (`db/session.py`) تا وقتی چند دستور گروهی هم‌زمان به دیتابیس می‌نویسن به‌جای خطای "database is locked" منتظر بمونن.

قانون اصلی: هندلرهای `bot/` هیچ‌وقت مستقیم فرمول بازی محاسبه نمی‌کنن — همیشه از `game/` صدا می‌زنن. اگه فاز بعد اومد (DNA splicing، rarity، جنگ قلمرو، بازار)، هرکدوم یه ماژول جدید توی `game/` می‌شه، نه اینکه فایل‌های موجود شلوغ بشن.

## اجرا

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env   # BOT_TOKEN رو از @BotFather بگیر و توی .env بذار
python -m bot.main
```

## دیتابیس

SQLite برای MVP (`game.db`, در `.gitignore`). اسکیمای دیتابیس با `init_db()` در `db/session.py` خودکار ساخته می‌شه — فعلاً migration جداگانه (Alembic) نداریم چون پروژه هنوز فاز اول‌شه؛ وقتی اسکیما پایدار شد اضافه می‌کنیم.

## وضعیت فعلی

- ✅ موجود اولیه تصادفی با عنصر (fire/water/earth/electric)
- ✅ تغذیه، تمرین (کول‌داون ۴ ساعته)، ارتقای ۴ عضو (بال/زره/نیش/غدد سمی)
- ✅ دوئل خودکار نوبتی با لاگ روایی و مزیت عنصری (`/duel`) — برای وقتی حریف آفلاینه
- ✅ نبرد تعاملی زنده با اسکیل مخصوص هر عنصر (`/battle`) — برای وقتی هر دو نفر پای بازی‌ان
- ✅ Raid Boss ساده با کول‌داون حمله و تقسیم غنایم بر اساس سهم دمیج
- ✅ لیدربورد و محافظ گروه — group-scoped، فقط اعضای همون گروه حساب می‌شن. محافظ (`Group.guardian_creature_id`) پایدار و واقعیه: `/guardian` نشون می‌ده کیه (و اولین بار خودکار قوی‌ترین رو تاج‌گذاری می‌کنه)، `/guardian_challenge` با یه دوئل واقعی عنوان رو می‌گیره، `/guardian_claim` جایزه‌ی روزانه‌ی محافظ فعلیه
- ✅ سیستم rarity کامل (common/rare/epic/legendary/mutant) با ضریب استت
- ✅ ترکیب DNA (splicing): دو موجود کاربر ترکیب می‌شن، والدین غیرفعال می‌شن (می‌مونن توی کلکسیون)، فرزند با شانس ارتقای rarity ساخته می‌شه
- ✅ کلکسیون (`/collection`) و تعویض موجود فعال (`/select`)
- ✅ ماموریت روزانه (`/missions`) و سقف انرژی روزانه روی تغذیه/حمله به رید/جایزه‌ی محافظ تا جلوی فارم بی‌نهایت گرفته بشه
- ✅ تبادل سکه/DNA/**مالکیت موجود** بین بازیکن‌ها (`/give coins`, `/give dna`, `/give creature`) و رویداد جهش محیطی یک‌بار-در-روز در هر گروه (`/mutation_event`)
- ✅ `/help` — لیست کامل دستورات، هم توی پیوی هم گروه

همه‌ی این‌ها با اسکریپت‌های smoke test (خارج از ریپو، در scratchpad) به‌صورت end-to-end و **cross-session** تست شدن — یعنی شبیه‌سازی چند session جدا مثل هندلرهای واقعی بات، نه فقط توی یه پروسه‌ی پیوسته. همین باعث شد یه باگ بحرانی (مقایسه‌ی datetime timezone-aware/naive که بعد از reload از SQLite کرش می‌کرد) و یه باگ HTML-parsing (تگ خام `<id>` توی پیام با `parse_mode="HTML"` که تلگرام ردش می‌کرد) قبل از هر deploy واقعی پیدا و رفع بشن. تست واقعی روی تلگرام هنوز انجام نشده — نیاز به `BOT_TOKEN` واقعی از @BotFather داره.

## فاز بعد (برنامه‌ریزی‌شده، هنوز پیاده نشده)

- جنگ قلمرو بین گروه‌ها (حمله‌ی یه گروه به قلمرو گروه دیگه؛ فعلاً guardian فقط داخل یه گروهه، بین دو تا گروه تلگرام مجزا هنوز مکانیزم نداریم)
- بازار عمومی (لیست کردن موجود/DNA برای فروش با قیمت‌گذاری، نه فقط هدیه‌ی مستقیم دو نفره)
- ماموریت هفتگی، رویدادهای فصلی
- Alembic برای migration وقتی اسکیما دیگه تغییر پرشتاب نداشت (فعلاً هر تغییر مدل یعنی باید `game.db` محلی رو پاک کنی و از نو بسازی)
