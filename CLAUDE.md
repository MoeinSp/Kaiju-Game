# Kaiju Bio-Lab — Telegram Chat Game

بات بازی تلگرام: رشد و ترکیب ژنتیکی هیولا در پیوی، دوئل/رید باس در گروه. دیتابیس و ادمین با **Django**، بات با **python-telegram-bot** (asyncio).

## ساختار پروژه (modular monolith)

- `telgame_site/` — تنظیمات پروژه‌ی Django. **انتخاب دیتابیس با یه سوییچ انجام می‌شه:** اگه `POSTGRES_DB` ست باشه کل اپ (بات + پنل) می‌ره روی Postgres، وگرنه همون SQLite قبلی می‌مونه. این عمدیه — تست‌های smoke و توسعه‌ی لوکال با `DJANGO_DB_PATH` کار می‌کنن و نباید مجبور به بالا آوردن Postgres بشن. مسیر `/` به پنل ریدایرکت می‌شه، `/panel/` پنل اپراتوره و `/admin/` همون ادمین خام جنگو می‌مونه.
- `panel/` — **پنل تحت وب اپراتور** (جدا از ادمین جنگو). ادمین جنگو ویرایشگر خامِ جدول‌هاست؛ این یکی کارمحوره: پالت رنگ دکمه‌ها، ایموجی‌های پرمیوم، لودآوت‌های ظاهر، پشتیبان‌گیری/بازیابی، بازیکنان، کانال‌های جوین اجباری. همه‌ی ویوها staff-only و همه یه شکل دارن: GET رندر می‌کنه، POST یه `action` نام‌دار رو اجرا می‌کنه و ریدایرکت می‌ده (post/redirect/get تا رفرش یه عملیات مخرب رو دوباره شلیک نکنه). **قانون:** پنل هیچ‌وقت مستقیم به ORM نمی‌نویسه، همیشه از همون توابع `game/` استفاده می‌کنه که بات استفاده می‌کنه — وگرنه اعتبارسنجی و باطل‌کردن کش بین دو تا فرانت‌اند از هم جدا می‌افتن. CSS به‌صورت inline توی `base.html` نوشته شده: پنل باید روی یه VPS خام و پشت هر ریورس‌پراکسی‌ای کار کنه، و یه فایل استاتیک گم‌شده نباید دلیل نرسیدن به دکمه‌ی بازیابی باشه.
- `bio_lab/` — اپ Django: `models.py` (تک منبع حقیقت برای اسکیما)، `admin.py` (پنل ادمین — همه‌ی مدل‌ها رجیستر شدن، یعنی از همون اول یه داشبورد آماری رایگان داری)، `repository.py` (کوئری‌های مشترک مثل get_or_create_user/group).
- `bot/` — لایه تلگرام. فقط parse کردن آپدیت، صدا زدن `game/`/`bio_lab/`، و فرستادن جواب. منطق بازی اینجا نباید باشه.
  - `bot/buttons.py` — **تنها جایی که دکمه ساخته می‌شه**. `btn()` سه چیز رو با هم مدیریت می‌کنه: (۱) `style` — که **یه نقش معناییه، نه یه رنگ** (`NAV`/`BACK`/`PRIMARY`/`CONFIRM`/`BUILD`/`SHOP`/`BATTLE`/`DANGER`/`ADMIN`) و از طریق `game/button_style.py` به رنگ واقعی ترجمه می‌شه، (۲) `icon_custom_emoji_id` — ایموجی پرمیوم قبل از متن، (۳) ایموجی یونیکد fallback داخل خودِ متن دکمه. نکته‌ی طراحی: **هیچ‌وقت رنگ نباید تنها تمایز دو دکمه باشه** — چون `style` فقط روی کلاینت‌های بعد از ۹ فوریه ۲۰۲۶ رندر می‌شه و روی قدیمی‌ترها دکمه بی‌رنگ می‌مونه؛ برای همین هر دکمه ایموجی یونیکد خودش رو هم توی متن نگه می‌داره. `back_btn()` عمداً بی‌رنگه (ناوبریه، نه اکشن). `SUCCESS` هنوز به‌عنوان اسم مستعار `CONFIRM` وجود داره تا کد قدیمی نشکنه.
  - `bot/utils.py` — `run_db()`: چون Django ORM sync ـه ولی بات روی asyncio اجرا می‌شه، هر تابع دیتابیسی/بازی رو با `sync_to_async` می‌پیچونه تا event loop بلاک نشه.
  - `bot/handlers/private.py` — پیوی. **کارت موجود (`creature_card_text`) عمداً کوتاهه**: هویت + یه ردیف استت + XP + نوار منابع. سطح اجزای بدن و دکمه‌های تغذیه/تمرین/ارتقا رفتن به صفحه‌ی جدای «🔧 ارتقا و پرورش» (`upgrade_panel`) — قبلاً همه توی یه پیام بودن و کارت غیرقابل‌خوندن شده بود. `wallet_line()` نوار منابع مشترکه که هر صفحه‌ای لازم داشته باشه ازش استفاده می‌کنه. اولین `/start` هر بازیکن **اسم آزمایشگاه** می‌پرسه (`User.lab_name`، یک‌بار برای همیشه) و تا وقتی نده کارت نشون داده نمی‌شه. بقیه‌ی تجربه هم **کاملاً دکمه‌ایه** (`/start`/`/menu` → کلکسیون، فیوژن، اتحاد، شبیخون، همه با دکمه‌های شیشه‌ای پیش می‌رن، نه آرگومان کامند). دستورات قدیمی با آرگومان (`/select 3`, `/fusion 3 5`, `/alliance_deposit 100`, `/heist اسم`, …) هنوز به‌عنوان میان‌بر کاربر حرفه‌ای رجیستر شدن ولی دیگه جایی تبلیغ/آموزش داده نمی‌شن. جریان‌هایی که ورودی متنی خالص لازم دارن (اسم اتحاد، مقدار واریز) از یه state ساده به اسم «awaiting» توی `context.user_data` استفاده می‌کنن (پایین‌تر توضیح داده شده).
  - `bot/handlers/inventory.py` — کوله‌پشتی تجهیزات، کاملاً دکمه‌ای: لیست آیتم‌ها → جزئیات هرکدوم → تجهیز/خارج‌کردن/ارتقا. `/inventory`/`/equip`/`/unequip`/`/upgrade_item` هم به‌عنوان میان‌بر کامندی باقی موندن.
  - `bot/handlers/lootbox.py` — `/biocrate`: باز کردن باکس ژنتیکی با طلا؛ نتیجه پشت `<tg-spoiler>` مخفی می‌شه تا لحظه‌ی باز کردنش شبیه قرعه‌کشی واقعی حس بشه. `/diamondbox` هم همینجاست: لیست چندسطحی جعبه‌های الماسی، هرکدوم با احتمال دقیق هر رده (٪) قبل از خرید نشون داده می‌شه.
  - `bot/handlers/buildings.py` — `/buildings`: ساختمون‌های idle (جمع‌کننده‌ی طلا/الماس)، جمع‌آوری، شروع ارتقا، و سرعت‌دادن با کارت. کاملاً دکمه‌ای.
  - `bot/handlers/wheel.py` — `/wheel`: گردونه‌ی شانس روزانه، یه چرخش رایگان در روز.
  - `bot/handlers/arena.py` — `/arena`: کاپ/سپر، پیدا کردن حریف، حمله، و جدول برترین‌ها. حریفِ نشون‌داده‌شده توی `context.user_data` نگه داشته می‌شه تا نبرد دقیقاً با همون حریفی که دیدی انجام بشه، نه یه حریف تازه قرعه‌کشی‌شده.
  - `bot/handlers/group.py` — دستورات گروه (`/duel` — با شرط‌بندی طلای اختیاری، `/raid_spawn`, `/attack`, `/leaderboard`, `/guardian`, `/guardian_challenge`, `/guardian_claim`, `/give`, `/mutation_event`). این‌ها **عمداً** کامند موندن — چون هدف‌گیری بازیکن با ریپلای‌کردن روی پیامشه، و Bot API راهی برای گرفتن لیست کامل اعضای گروه (تا بشه دکمه‌ای انتخابشون کرد) نمی‌ده.
  - `bot/handlers/battle.py` — نبرد تعاملی/اسکیل‌دار (`/battle`) با دکمه‌های شیشه‌ای نوبت‌به‌نوبت.
  - `bot/handlers/misc.py` — `/help` (کار می‌کنه هم توی پیوی هم گروه).
  - `bot/handlers/welcome.py` — پیام خوش‌آمد + درخواست ادمین وقتی بات به یه گروه اضافه می‌شه (`ChatMemberHandler` روی `my_chat_member`).
  - `bot/handlers/owner.py` — پنل مدیریت، چک‌شده با `config.OWNER_TELEGRAM_ID`، الان کاملاً دکمه‌ایه: `/admin` یه داشبورد با دکمه‌های «👤 مدیریت کاربر» (اطلاعات/اعطا/کسر/مسدودسازی — هرکدوم با یه پرامپت متنی کوتاه)، «🗑 حذف موجود»، «📢 ارسال همگانی»، «🎨 ایموجی پرمیوم» (`/set_emoji`ی قدیمی)، و «📡 جوین اجباری» (پایین‌تر). دستورات قدیمی (`/user_info`, `/grant`, `/deduct`, `/ban`, `/unban`, `/delete_creature`, `/broadcast`, `/set_emoji`, …) هم به‌عنوان میان‌بر باقی موندن.
  - `bot/middleware.py` — دو `TypeHandler` که روی **همه‌ی** آپدیت‌ها، قبل از هر هندلر دیگه، به ترتیب اجرا می‌شن (هرکدوم توی `group` عددی جدا، چون PTB توی هر گروه فقط اولین هندلری که match بشه رو اجرا می‌کنه — پایین‌تر توضیح داده شده):
    1. `enforce_ban` (`group=-2`) — اگه کاربر `is_banned` باشه با `ApplicationHandlerStop` جلوی رسیدنش به بقیه رو می‌گیره.
    2. `enforce_force_join` (`group=-1`) — اگه یه `RequiredChannel` فعال هست که کاربر عضوش نیست، به‌جای هندلر واقعی یه پیام «باید عضو بشی» با دکمه‌ی جوین + «بررسی مجدد» نشون می‌ده و متوقف می‌شه؛ وگرنه (کاملاً عضو) پاداش هر کانالی که تازه رد کرده رو یک‌بار می‌ده و اجازه می‌ده ادامه پیدا کنه. برای اینکه هر آپدیت `get_chat_member` (یه API call واقعی تلگرام) صدا نزنه، توی `context.user_data["force_join_passed_ids"]` کش می‌شه که کاربر برای کدوم کانال‌ها قبلاً تأیید شده.
- `game/` — منطق خالص بازی. از Django ORM استفاده می‌کنه (چون مدل‌ها Django‌ان) ولی از تلگرام کاملاً بی‌خبره — قابل تست مستقل با یه اسکریپت که `django.setup()` می‌زنه.
  - `constants.py` — عناصر، اجزای بدن، rarity، اسکیل‌ها، هزینه‌ها، فرمول‌ها. **`SPECIES` تک‌منبع حقیقتِ گونه‌هاست** و نگاشتش دقیقاً یک‌به‌یکه: هر اسم (از اساطیر ایران — شاهنامه/اوستا) دقیقاً به یک عنصر تعلق داره و هیچ اسمی زیر دو عنصر تکرار نشده. این تزئینی نیست — `name` کلید هویت فیوژنه، پس اگه دو گونه‌ی متفاوت هم‌اسم می‌شدن، دو موجود بی‌ربط با هم ترکیب می‌شدن. `SPECIES_NAMES` (عنصر → لیست اسم) از روی همین ساخته می‌شه تا هیچ‌وقت از هم جدا نیفتن، و `species_element()` جهت معکوس رو می‌ده. migration `0012_normalize_species_names` رکوردهای قدیمی دیتابیس (که اسم‌های انگلیسی/نامعتبر داشتن) رو به گونه‌ی معتبرِ هم‌عنصر خودشون منتقل کرد.
  - `creature.py` — ساخت موجود، استت مؤثر، تغذیه/تمرین/ارتقا، لول‌آپ، کلکسیون، جهش تصادفی. `effective_stats` یه ضریب کوچیک بر اساس `star_level` (پایین‌تر) روی hp/atk/def/spdِ خودِ موجود اعمال می‌کنه — نه روی بونوس تجهیزات، چون ستاره یه پرستیژ خودِ موجوده نه گیرش.
  - `combat.py` — دوئل خودکار (نوبتی، بر اساس SPD/ATK/DEF/عنصر) + لاگ متنی. برای وقتی حریف آفلاینه یا حوصله‌ی تعامل نیست. `resolve_duel` خودش `game.equipment.get_equipped_items()` رو برای هر دو طرف صدا می‌زنه (چون همیشه توی sync context صدا زده می‌شه)، پس بونوس تجهیزات (کریتیکال از سلاح، جون‌خواری از طلسم) همیشه توی نتیجه لحاظ می‌شه. **فرمت لاگ:** دقیقاً یه خط به‌ازای هر حمله — زهر/جون‌خواری/کریتیکال/مزیت عنصری همه به‌صورت پسوند کوتاه توی همون خط میان، نه خط جدا؛ خلاصه‌ی HP فقط یک‌بار آخر لاگ میاد نه بعد هر راند. قبلاً هرکدوم خط جدا داشتن و لاگ یه دوئل ساده ۱۵+ خط می‌شد که عملاً غیرقابل‌خوندن بود.
  - `interactive_battle.py` — نبرد تعاملی زنده با اسکیل مخصوص هر عنصر (آتش=دمیج مضاعف، آب=شفا، خاک=سپر، برق=استان). state روی `InteractiveBattle` نگه داشته می‌شه چون بین چند callback پخش می‌شه؛ توابعش فقط شیء رو mutate می‌کنن، ذخیره‌سازی (`.save()`) با caller (هندلر) ـه.
  - `raid.py` — اسپان باس، حمله با کول‌داون (دمیج شامل بونوس تجهیزات، `effective_stats(creature, get_equipped_items(creature))`)، تقسیم غنایم بر اساس سهم دمیج.
  - `hunt.py` — شکار انفرادی **دومرحله‌ای**: `scout_targets()` سه حریف (یکی به‌ازای هر رده‌ی `HUNT_TIERS`: ضعیف/هم‌سطح/قوی) با قدرت و غنیمت تخمینی برمی‌گردونه تا بازیکن **قبل از خرج کردن انرژی** ریسک رو ببینه؛ بعد `resolve_hunt(user, creature, tier, seed)` همون حریف رو دوباره می‌سازه و می‌جنگه. **نکته‌ی کلیدی:** هر حریف با یه `seed` ساخته می‌شه که توی `callback_data` حمل می‌شه — بدون این، حریفی که توی پیش‌نمایش نشون دادیم با حریفی که واقعاً باهاش می‌جنگیم فرق می‌کرد و کل «انتخاب حریف» بی‌معنی می‌شد. رده‌های سخت‌تر هم استت بالاتر دارن هم غنیمت بیشتر (`stat_mult`/`reward_mult`)، پس انتخاب واقعاً یه تصمیم ریسک/پاداشه.
    موجود وحشی هیچ‌وقت save نمی‌شه (`id` همیشه `None`ـه، فقط برای محاسبه‌ی نبرد). **نکته‌ی مهم:** چون save نمی‌شه، `game.equipment.get_equipped_items()` باید صریحاً برای `creature.pk is None` یه لیست خالی برگردونه وگرنه Django روی فیلتر با یه instance ذخیره‌نشده کرش می‌کنه (`ValueError: Model instances passed to related filters must be saved`) — این باگ واقعی رخ داد و رفع شد.
  - `fusion.py` (قبلاً `splice.py`) — **فیوژن**: سوزوندن (حذف واقعی، نه غیرفعال‌سازی) دو موجود + هزینه‌ی طلا (`FUSION_GOLD_COST`) → یه موجود یک ستاره بالاتر. **قانون سخت‌گیرانه‌ی ستاره:** هر دو والد باید **هم‌نام (هم‌گونه) و هم‌ستاره** باشن؛ فرزند `star_level + 1` می‌گیره (سقف `STAR_MAX`)، **همون اسم والدین** رو نگه می‌داره، و **مجموع XP هر دو والد** رو به ارث می‌بره. دلیل این محدودیت: بدون اون، رسیدن به ۵ ستاره یه عارضه‌ی جانبیِ ترکیب هرچی دم دست بود می‌شد، نه یه هدف کلکسیونی. **فیوژنِ جفت معتبر هیچ قرعه‌کشی شکستی نداره — هم‌نام + هم‌ستاره یعنی ۱۰۰٪ موفق**؛ تنها چیزهایی که می‌تونن جلوش رو بگیرن شرط‌های صریحن (ساخته‌نبودن تالار ادغام، رسیدن به سقف ستاره، طلای ناکافی). دو تابع، UI رو از پیشنهاد جفت نامعتبر بازمی‌دارن: `fusion_partners(user, creature)` شرکای مجازِ **یک** موجود رو می‌ده، و `ready_pairs(user)` همه‌ی گروه‌های (گونه، ستاره) که بازیکن حداقل دوتاش رو داره — یعنی هر جفتی که همین الان قابل ترکیبه — مرتب‌شده بر اساس ستاره، با بالاترین‌لِوِل به‌عنوان `parent_a`. این دومی صفحه‌ی «🧪 ترکیب هیولا» رو تغذیه می‌کنه. **باگ واقعی که این دو تابع رفعش کردن:** انتخابگر فیوژن قبلاً *همه‌ی* موجودهای بازیکن رو به‌عنوان شریک نشون می‌داد و بعد `fuse()` تقریباً هر انتخابی رو رد می‌کرد — از دید بازیکن «ترکیب کار نمی‌کنه». حالا هر دکمه‌ای که رندر می‌شه تضمین‌شده جواب می‌ده، و وقتی شریکی وجود نداره هندلر **دلیلش** رو می‌گه (تالار ساخته نشده / سقف ستاره / جفت هم‌نام نداری) نه یه «شریکی نیست» خالی. علاوه بر این، شانس ارتقای rarity (`RARITY_UPGRADE_CHANCE`) و ۵۰٪ شانس (`FUSION_INHERIT_CHANCE`) به‌ارث‌بردن یه تجهیزات از والدین هم هست. با `@transaction.atomic`. فرزند `is_active=True` می‌شه و بقیه `is_active=False` (قانون تک‌موجود فعال، پایین‌تر).
  - `season.py` — **فصل هفتگی کاپ**. جدول آرنا هر هفته (دوشنبه ۰۰:۰۰ UTC) ریست می‌شه تا کسی که دیر شروع کرده برای همیشه از صدر جدول محروم نباشه — ولی ریست به صفر یعنی دور ریختن یه هفته تلاش، پس هر بازیکن به یه **کف متناسب با رتبه‌ش** برمی‌گرده (`SEASON_RANK_FLOORS`) به‌علاوه‌ی برشی از مازادش (`SEASON_CARRYOVER_PCT`). `reset_floor()` هیچ‌وقت بیشتر از کاپ واقعی برنمی‌گردونه — ریست نباید ترفیع باشه. تسویه هم مثل بقیه‌ی بازی **lazy**ـه: هیچ cronی نیست، `close_due_season()` روی هر خوندن آرنا/جدول صدا زده می‌شه و اگه هفته عوض شده باشه یه بار تسویه می‌کنه؛ `SeasonState` این رو idempotent نگه می‌داره.
  - `arena.py` — **آرنای کاپ (PvP)**: مچ‌میکینگ بر اساس `User.cup` داخل یه باند (`ARENA_MATCH_CUP_BAND`)؛ اگه هیچ بازیکن واقعیِ بدون‌سپری توی باند نبود، یه **حریف فیک** با اسم آزمایشگاه ساختگی (`ARENA_FAKE_LAB_NAMES`) و لوت نوسانی ساخته می‌شه تا آرنا روی جمعیت کم هم خالی نمونه. برد یعنی `ARENA_LOOT_PERCENT` (۱۰٪) طلای مدافع (با کف `ARENA_LOOT_MIN` تا غارت یه بازیکن بی‌پول هم بی‌ارزش نباشه). **دو مکانیزم ضدفارم:** (۱) مدافع بعد از خوردن حمله `ARENA_SHIELD_HOURS` (۸ ساعت) سپر می‌گیره و از مچ‌میکینگ خارج می‌شه، (۲) خودِ حمله‌کردن سپر خودت رو می‌سوزونه، پس نمی‌شه پشت سپر قایم شد و هم‌زمان بقیه رو غارت کرد. **`cup_delta()` مهم‌ترین بخشه:** بردن از کسی که کاپ بالاتری داره بیشتر می‌ده و باختن به کسی که ضعیف‌تره بیشتر می‌گیره؛ از اون مهم‌تر، یه **سقف نرم بر اساس قدرت واقعی** هست (`deserved_cup()` = `power * ARENA_CUP_PER_POWER`) — اگه کاپ بازیکن از چیزی که قدرتش توجیه می‌کنه جلو بزنه، بردهاش با `ARENA_OVERCAP_DAMPING` ضرب می‌شه. بدون این، یه بازیکن ضعیف با یه دست شانسی می‌رفت توی براکتی که تا ابد ازش کتک می‌خورد — دقیقاً همون چیزی که نباید بشه.
  - `blacksmith.py` — **آهنگری**: ارتقای سطح تجهیزات فقط با طلا، بدون نیاز به نمونه‌ی تکراری. عمداً ریسک داره: تا `FORGE_SAFE_LEVEL` بدون شکسته، بعدش هر سطح `FORGE_FAIL_CHANCE_PER_LEVEL` شانس شکست اضافه می‌کنه (سقف `FORGE_MAX_FAIL_CHANCE`) و در صورت شکست طلا می‌سوزه ولی سطح بالا نمی‌ره. این عمدیه تا `game.equipment.upgrade_item` (مصرف نمونه‌ی تکراری) مسیر امن‌ولی‌کند بمونه و آهنگری مسیر سریع‌ولی‌پرریسک. **آهنگری یه ساختمونه و باید ساخته بشه**: `equipment_cap(user)` = `blacksmith_level * EQUIPMENT_LEVELS_PER_BLACKSMITH_LEVEL` (پس L1 → +۵ و L5 → +۲۵ که همون `EQUIPMENT_MAX_LEVEL`ـه). **هر دو مسیر ارتقا** (این و `equipment.upgrade_item`) از همین سقف پیروی می‌کنن، وگرنه بازیکن بدون آهنگری می‌تونست از مسیر نمونه‌ی تکراری دورش بزنه.
  - `equipment.py` — تجهیزات: ۴ اسلات (`weapon`/`armor`/`rune`/`offhand`)، هر اسلات یه بونوس پایه (`EQUIPMENT_BASE_BONUS`) که با rarity (`RARITY_STAT_MULTIPLIER`، همون جدول موجودها) و سطح ارتقا (`+۱` تا `+۱۰`، هر سطح `EQUIPMENT_UPGRADE_BONUS_PCT` بیشتر) اسکیل می‌شه. `equip_item`/`unequip_item` تضمین می‌کنن حداکثر یه آیتم در هر اسلات روی هر موجود مجهز باشه. `upgrade_item` یه نمونه‌ی هم‌نوع (اسلات+مدل+rarity یکسان) رو مصرف می‌کنه. «زهر» (poison) به‌عنوان مکانیزم DoT برای غلاف (offhand) استفاده مجدد شده، نه یه سیستم جدا.
  - `lootbox.py` — باز کردن Bio-Crate: هزینه‌ی ثابت طلا (`BIOCRATE_GOLD_COST`)، rarity با وزن‌های `LOOTBOX_RARITY_WEIGHTS` (یه جدول احتمال کاملاً جدا از `RARITY_UPGRADE_CHANCE`ی فیوژن) قرعه‌کشی می‌شه، بعد ۵۰٪ (`BIOCRATE_CREATURE_CHANCE`) موجود یا تجهیزات می‌ده. **موجودهای حاصل همیشه `is_active=False` ساخته می‌شن** — همون قانون تک‌موجود-فعال پایین‌تر. `open_diamond_box(user, tier)` جعبه‌ی الماسی چندسطحی رو باز می‌کنه — همیشه یه موجود می‌ده (نه تجهیزات)، با جدول وزن `DIAMOND_BOX_TIERS[tier]["weights"]` که کاملاً جدای بقیه‌ی جدول‌های rarityه؛ `roll_rarity()` یه پارامتر اختیاری `weights` گرفته تا این دو تابع همون قرعه‌کشی رو با جدول‌های متفاوت به اشتراک بذارن.
  - `buildings.py` — **ستون فقرات پیشرفت**. `Building` (نوع، سطح، `last_collected_at`) و `BuildingUpgrade` (فقط یه ردیف به‌ازای هر owner — `OneToOneField` همین «یه کارگر همزمان» رو در سطح دیتابیس تضمین می‌کنه، نه فقط توی کد).
    - **`level == 0` یعنی «هنوز ساخته نشده»**؛ تنها ساختمونی که از اول سطح ۱ داره `main_hall`ـه. اولین «ارتقا»ی هر ساختمون دیگه در واقع ساختِ اونه.
    - **قانون مرکزی: هیچ ساختمونی نمی‌تونه از سطح `main_hall` جلو بزنه** (`max_level_for()`). این تنها گلوگاه عمدی بازیه — همه‌چیز پشت ارتقای تالار صف می‌کشه. سقف مطلق همه `BUILDING_MAX_LEVEL = 5`ـه.
    - ۶ نوع ساختمون: `main_hall` (تالار مِهر — گلوگاه + سقف ستاره)، `gold_collector`/`diamond_collector`/`dna_lab` (تولیدکننده)، `blacksmith` (سقف سطح تجهیزات)، `fusion_lab` (پیش‌نیاز فیوژن). ساختمون‌هایی که توی `BUILDING_PRODUCTION` نیستن **دروازه**ان نه تولیدکننده — `produces()` این دو رو از هم جدا می‌کنه و `pending_amount()` برای اونا و برای ساخته‌نشده‌ها صفر می‌ده.
    - `star_cap(user)` = سطح تالار اصلی → سقف ستاره‌ی هیولاهای اون بازیکن.
    - تولید مثل `energy.py` **lazy**ـه: `pending_amount()` از `last_collected_at` تا الان حساب می‌کنه، سقفش `cap_base * level`. `check_and_apply_upgrade` هروقت صدا زده بشه اگه زمانش گذشته باشه سطح رو بالا می‌بره — بدون هیچ cron/background job. `SpeedupCard` با `apply_speedup` مصرف می‌شه و از `finishes_at` کم می‌کنه. `maybe_award_speedup_card()` یه شانس کوچیک کارت از فعالیت‌های عادی می‌ده.
  - `wheel.py` — گردونه‌ی شانس روزانه: `spin()` با همون الگوی `ENERGY_CAPS`ی `daily.py` به یک‌بار در روز محدوده (`ENERGY_CAPS["wheel_spin"]`)، جایزه از جدول وزن‌دار `WHEEL_PRIZES` (طلا/DNA/الماس/کارت سرعت) قرعه‌کشی می‌شه.
  - `daily.py` — ماموریت روزانه، پاداش ورود متوالی (streak)، رویداد گروهی یک‌بار-در-روز، و `ENERGY_CAPS`/`assert_energy_available` برای چیزهایی که واقعاً باید فقط یک‌بار در روز باشن (نه grind-پذیر) مثل جایزه‌ی محافظ.
  - `energy.py` — استامینای تجدیدشونده (نه سقف روزانه): هر `ENERGY_REGEN_MINUTES` دقیقه یه واحد شارژ می‌شه تا سقف `MAX_ENERGY`. محاسبه lazy ـه — روی هر خوندن/خرج‌کردن انرژی، از `energy_updated_at` تا الان حساب می‌شه، نه با یه cron جدا.
  - `guardian.py` — محافظ گروه پایدار (روی `Group.guardian_creature`)، نه فقط نمایشی: با `/guardian_challenge` واقعاً می‌شه عنوان رو با دوئل گرفت.
  - `trade.py` — هدیه دادن مالکیت یه موجود به بازیکن دیگه.
  - `alliance.py` — اتحاد بین بازیکن‌ها، مستقل از گروه تلگرام (روی `User.alliance`، نه `GroupMembership`) — یعنی اعضای یه اتحاد می‌تونن از گروه‌های مختلف تلگرام باشن. رهبری خودکار منتقل می‌شه اگه رهبر بره؛ اگه آخرین عضو بره، اتحاد حذف می‌شه. `deposit_treasury` طلای بازیکن رو به خزانه‌ی اتحاد (`Alliance.treasury_gold`) منتقل می‌کنه. `heist` به خزانه‌ی یه اتحاد **دیگه** شبیخون می‌زنه: اگه اون اتحاد موجود فعال داشته باشه، `combat.resolve_duel` بین قوی‌ترین موجود مدافع و مهاجم اجرا می‌شه (فقط با بردن، طلا دزدیده می‌شه)؛ اگه هیچ نگهبانی نداشته باشه، شبیخون خودکار موفق می‌شه. کول‌داون (`HEIST_COOLDOWN_HOURS`) روی خودِ اتحاد مدافعه (`Alliance.last_heisted_at`)، و سقف تلاش روزانه‌ی مهاجم (`HEIST_DAILY_ATTEMPTS`) با همون مکانیزم `ENERGY_CAPS`ی `daily.py` اعمال می‌شه (نه داخل خودِ `heist`).
  - `emoji.py` — نگاشت `key -> Telegram Premium custom emoji`، با یه cache درون‌حافظه‌ای (`refresh_cache()` بعد از هر تغییر) تا `get_emoji()` بدون کوئری دیتابیس از context همزمان (async) هم صدا زدنی باشه. `EMOJI_DEFS` تک منبع حقیقته (کلید → برچسب، ایموجی پیش‌فرض، دسته) — ۴۴ کلید توی ۸ دسته، پوشش تقریباً کل آیکون‌های تکرارشونده‌ی بازی (نه فقط منابع). کلیدهایی که پیش‌فرضشون یکی‌ان ولی مفهوماً فرق دارن (مثلاً `energy` و `element_electric` هر دو ⚡ پیش‌فرضن) کاملاً مستقل از هم قابل تنظیمن.
  - `button_emoji.py` — ایموجی پرمیومِ **روی دکمه‌ها**، عمداً جدا از `emoji.py`. دلیل جدا بودن: این دو مکانیزم متفاوتن — ایموجی متن با `<tg-emoji>` داخل HTML رندر می‌شه، ولی آیکون دکمه یه فیلد خالیِ `icon_custom_emoji_id` روی خودِ `InlineKeyboardButton`ـه و **هر دکمه فقط یکی می‌گیره**. چون فقط یکی جا می‌شه، کلیدها *دکمه* رو توصیف می‌کنن نه *مفهوم* رو (`btn_attack` یعنی «دکمه‌ی حمله»، نه «ایموجی نبرد») تا سازنده بتونه آیکون دکمه رو مستقل از ایموجی متنِ متناظرش تنظیم کنه. **`_cache` اینجا هیچ‌وقت lazy پر نمی‌شه** (برخلاف `emoji.py`) — دکمه‌ها داخل هندلرهای async ساخته می‌شن و یه لود تنبل اینجا `SynchronousOnlyOperation` می‌داد؛ یه آیکون صرفاً تزئینی هرگز نباید بتونه یه هندلر رو بترکونه، پس کش سرد یعنی «فعلاً آیکون سفارشی نداریم» و به fallback یونیکد برمی‌گرده. `bot/main.py` موقع استارت گرمش می‌کنه.
  - `button_style.py` — **معنیِ رنگ‌ها**. تلگرام فقط سه رنگ دکمه داره، پس چیزی که قابل تنظیمه «رنگ این دکمه» نیست، بلکه «کدوم *نقش* چه رنگیه»: `ROLE_DEFS` نه تا نقش معنایی رو تعریف می‌کنه (ناوبری، بازگشت، کنش اصلی، تأیید، ساخت، خرید، نبرد، مخرب، مدیریت) و `resolve_style(role)` هر نقش رو به رنگ فعلی‌ش ترجمه می‌کنه. **پالت پیش‌فرض عمداً ناوبری رو بی‌رنگ گذاشته** — نسخه‌ی قبلی همه‌ی دکمه‌های منو رو آبی می‌کرد و نتیجه یه دیوار آبی بود که دو تا دکمه‌ی سبز توش گم می‌شد؛ رنگ عملاً هیچ اطلاعاتی حمل نمی‌کرد. حالا رنگ فقط روی کاری که بازیکن انجام می‌ده می‌شینه. `ButtonStyleOverride` **فقط برای نقش‌هایی که با پیش‌فرض فرق دارن** ردیف داره، پس جدول خالی یعنی «پالت استاندارد» و برگشت به پیش‌فرض با حذف انجام می‌شه نه با ذخیره‌ی یه کپی از پیش‌فرض. همون قانون کش `button_emoji.py` اینجا هم برقراره: کش هیچ‌وقت lazy پر نمی‌شه.
  - `theme.py` — **لودآوت‌های ظاهر**. یه اسنپ‌شات JSON از ایموجی متن‌ها + ایموجی دکمه‌ها + پالت رنگ. **هیچ داده‌ی بازی داخلش نیست** و این عمدیه: سوییچ لودآوت باید یه کار امن و برگشت‌پذیر باشه؛ اگه می‌تونست طلای کسی رو هم برگردونه، دیگه سوییچ ظاهر نبود، بازیابی بود و هیچ‌کس جرئت نمی‌کرد بزنتش (بازیابی واقعی توی `backup.py` با تأیید جدا). `apply_snapshot()` یه **جایگزینی کامله نه merge** — وگرنه لودآوت‌ها روی هم انباشته می‌شدن و بعد از سوییچ به یه تم مینیمال هنوز آیکون‌های تم قبلی رو داشتی. `validate_snapshot()` مرز اعتماده چون فایل import از بیرون میاد. `refresh_theme_caches()` هم تنها جاییه که هر سه کش با هم گرم می‌شن (`bot/main.py` موقع استارت همینو صدا می‌زنه).
  - `backup.py` — **پشتیبان‌گیری/بازیابی کل دیتابیس**. فرمت: یه JSON فشرده‌شده (`.json.gz`) شامل manifest + خروجی سریالایزر جنگو. **عمداً دامپ باینری نیست** — کل هدف اینه که پشتیبانِ گرفته‌شده روی SQLite لوکال، روی Postgres سرور بازیابی بشه، و این فقط با یه دامپ engine-neutral ممکنه. `contenttypes` و `auth.permission` حذف می‌شن چون `migrate` خودش می‌سازتشون و نگه‌داشتنشون موقع بازیابی به تصادم primary key می‌خوره؛ `auth.User` **هست** تا بازیابی روی یه سرور تازه، حساب‌های ورود پنل رو هم بیاره. `restore_payload()` اول **همه چیز رو deserialize می‌کنه و بعد flush می‌زنه**، همه توی یه تراکنش — یعنی یه فایل خراب باعث می‌شه بازیابی قبل از پاک‌شدن هر چیزی شکست بخوره. `resolve_backup()` مرز امنیتیه: اسم فایل از فرم میاد و بدون چک containment یه `../../` می‌تونست هر فایلی رو بخونه یا پاک کنه.
  - `report.py` — داشبورد آماری + `find_suspicious_activity()`: هر اکشن انرژی‌محور (feed/raid_attack/hunt) یه سقف نظری روزانه داره (کل انرژی پایه + همه‌ی تیک‌های regen ممکن در ۲۴ ساعت، تقسیم بر هزینه‌ی انرژی اکشن) — اگه `DailyActionLog.count` کسی از این سقف رد بشه یعنی چک انرژی یه‌جوری دور زده شده (باگ یا دستکاری مستقیم دیتابیس)، نه لزوماً «تقلب» به‌معنای عام؛ `/report` همینا رو نشون می‌ده.
  - `moderation.py` — ابزارهای مدیریتی: `resolve_user` (از `bio_lab/repository.py`، با آیدی عددی یا `@username` کاربر رو پیدا می‌کنه)، `grant_resource`/`deduct_resource` (تک‌منبعی، با کف صفر؛ `GRANT_RESOURCE_FIELDS` هر سه منبع رو پوشش می‌ده: `coins`/`dna`/`diamonds`)، `charge_user` (**شارژ چندمنبعی در یک فراخوانی** — پشت دکمه‌ی «⚡ شارژ کامل» پنل ادمین و دستور `/charge`؛ عدد منفی هم قبول می‌کنه برای کسر، هر منبع جدا کف صفر داره، و منابعی که مقدارشون صفر داده شده اصلاً دست نمی‌خورن)، `set_banned`، `delete_creature` (واقعاً حذف می‌کنه، نه غیرفعال — به همین خاطر توی بات پشت یه تأیید دکمه‌ای قرار داره).
  - `force_join.py` — جوین اجباری: `RequiredChannel` (کانال، `expires_at` نال‌پذیر = نامحدود، و جایزه‌ی سه‌گانه‌ی `reward_coins`/`reward_dna`/`reward_diamonds`) و `ChannelJoinClaim` (جلوگیری از گرفتن مجدد جایزه با ترک و رجوین). `active_channels()` فقط کانال‌های منقضی‌نشده رو برمی‌گردونه؛ `grant_reward_if_unclaimed()` idempotentه — با `IntegrityError` روی `unique(user, channel)` تشخیص می‌ده قبلاً گرفته شده یا نه، نه با یه چک جدا (race-safe). عضویت‌سنجی واقعی (`get_chat_member`) توی `bot/middleware.py`ه، چون یه API call تلگرامه نه کوئری دیتابیس — همونجا هم پیام گیت، جایزه‌ی هر کانال رو **قبل از عضو شدن** نشون می‌ده و بعد از تأیید عضویت، دقیقاً چیزی که گرفته شد رو گزارش می‌کنه.

**الگوی هندلرهای بات:** چون Django ORM sync ـه، هر هندلر به یه تابع sync داخلی (`_xxx_sync`) تقسیم می‌شه که همه‌ی کار دیتابیس/بازی رو می‌کنه و یا نتیجه برمی‌گردونه یا `GameError`/`RaidError` می‌ندازه؛ هندلر async فقط `await run_db(_xxx_sync, ...)` می‌زنه و جواب رو با تلگرام رد و بدل می‌کنه. پیام خطا همیشه از طریق exception میاد، نه return زودهنگام — این یعنی توابع sync قابل تست مستقل‌ان بدون لمس تلگرام.

قانون اصلی: هندلرهای `bot/` هیچ‌وقت مستقیم فرمول بازی محاسبه نمی‌کنن — همیشه از `game/` صدا می‌زنن. اگه فاز بعد اومد (جنگ قلمرو، بازار)، هرکدوم یه ماژول جدید توی `game/` می‌شه، نه اینکه فایل‌های موجود شلوغ بشن.

**قانون «تک موجود فعال»:** `bio_lab.repository.get_active_creature()` فرض می‌کنه دقیقاً یه ردیف `is_active=True` به ازای هر owner وجود داره. هر جایی که یه `Creature` جدید با `is_active=True` ساخته می‌شه (یا فرزند فیوژن) باید همه‌ی موجودهای دیگه‌ی همون owner رو `is_active=False` کنه (همون الگوی `set_active_creature`: `Creature.objects.filter(owner=user).exclude(id=keep_id).update(is_active=False)`). موجودهای حاصل از باکس ژنتیکی مستقیماً `is_active=False` ساخته می‌شن تا اصلاً این مسئله پیش نیاد. این باگ واقعاً یه‌بار رخ داد (باز کردن باکس، موجود فعال قبلی رو دوکپی کرد) و روی دیتابیس واقعی دستی هم رفع شد؛ `create_starter_creature` هم یه لایه‌ی self-healing داره (موجودهای دیگه رو هم غیرفعال می‌کنه) برای وقتی که این قانون به هر دلیلی قبلاً نقض شده باشه.

**قانون «یه MessageHandler متنی به ازای هر ChatType»:** python-telegram-bot توی هر عدد `group` فقط **اولین** هندلری که با آپدیت match بشه رو اجرا می‌کنه، بعد می‌ره سراغ `group` بعدی — این یعنی دو تا `MessageHandler` جدا با فیلترهای هم‌پوشان (مثلاً یکی برای همه‌ی پیوی، یکی فقط برای owner) اگه توی همون `group` باشن، دومی هیچ‌وقت اجرا نمی‌شه. به همین دلیل، تمام جریان‌های «منتظر یه پیام متنی/فوروارد بعدی‌ام» (اسم اتحاد، مقدار واریز، آیدی کاربر برای مدیریت، خودِ ایموجی پرمیوم، فوروارد کانال برای جوین اجباری، …) به‌جای هرکدوم یه `MessageHandler` جدا، از یه state ساده به اسم `context.user_data["awaiting_..."]` استفاده می‌کنن (هر بخش کلید خودشو داره: `private.AWAITING_PLAYER_KEY`, `owner.AWAITING_ADMIN_KEY`, `owner.AWAITING_FORCE_JOIN_KEY`, `owner.py`ی قدیمی `"awaiting_emoji_key"`) و **یک** `MessageHandler` مرکزی (`bot/main.py`ی `_capture_private_text_reply`) به ترتیب هرکدوم رو چک می‌کنه و به دیسپچر مربوطه (`private.capture_player_text_reply`, `owner.capture_owner_text_reply`) پاس می‌ده. اگه یه جریان متنی جدید اضافه کردی، یه کلید `awaiting_*` جدید بساز و برو توی همین دیسپچرهای موجود اضافه‌ش کن — یه `MessageHandler` جدید و جدا رجیستر نکن، چون بی‌صدا کار نمی‌کنه (نه ارور می‌ده، نه چیزی — فقط پیام کاربر رو هیچ‌کس جواب نمی‌ده).

## اجرا

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env       # BOT_TOKEN رو از @BotFather بگیر، DJANGO_SECRET_KEY رو هم عوض کن
python manage.py migrate     # ساخت اسکیمای دیتابیس
python manage.py createsuperuser   # اختیاری: برای ورود به پنل ادمین
python -m bot.main
```

پنل ادمین (بعد از `python manage.py runserver`) روی `http://127.0.0.1:8000/admin/` — همه‌ی مدل‌ها (کاربرها، موجودها، رید/دوئل لاگ‌ها، ماموریت‌ها) از همون اول قابل مشاهده و فیلتر کردنن؛ نیازی به ساختن داشبورد جدا برای آمار نیست. دستور `/admin` توی بات هم یه لینک به همین آدرس می‌ده (`config.ADMIN_PANEL_URL`، پیش‌فرض همون `127.0.0.1`) — این لینک فقط از همون سیستمی که سرور روش اجرا می‌شه باز می‌شه؛ برای دسترسی از گوشی باید `ADMIN_PANEL_URL` رو روی IP شبکه یا یه تانل عمومی (ngrok و مشابه) تنظیم کنی.

**PyCharm:** توی `.run/` پنج تا Run Configuration آماده‌ست (خودکار شناسایی می‌شن): Run Bot، Django Run Server، Django Migrate، Django Make Migrations، Django Create Superuser.

## دیتابیس

SQLite (`game.db`, در `.gitignore`، همون فایلی که همیشه بوده). اسکیما با Django migrations مدیریت می‌شه (`bio_lab/migrations/`) — دیگه نیازی به پاک کردن دستی `game.db` بعد از هر تغییر مدل نیست: فقط `makemigrations` بزن و بعد `migrate`. `USE_TZ = True` یعنی همه‌ی `DateTimeField`ها timezone-aware ذخیره می‌شن — کلاس باگ قبلی (مقایسه‌ی naive/aware datetime) با همین تنظیم از ریشه دیگه ممکن نیست.

## وضعیت فعلی

- ✅ موجود اولیه تصادفی با عنصر (fire/water/earth/electric)
- ✅ تغذیه، تمرین (کول‌داون ۴ ساعته)، ارتقای ۴ عضو (بال/زره/نیش/غدد سمی)
- ✅ دوئل خودکار نوبتی با لاگ روایی و مزیت عنصری (`/duel`) — برای وقتی حریف آفلاینه
- ✅ نبرد تعاملی زنده با اسکیل مخصوص هر عنصر (`/battle`) — برای وقتی هر دو نفر پای بازی‌ان
- ✅ Raid Boss ساده با کول‌داون حمله و تقسیم غنایم بر اساس سهم دمیج
- ✅ لیدربورد و محافظ گروه — group-scoped، فقط اعضای همون گروه حساب می‌شن. محافظ (`Group.guardian_creature_id`) پایدار و واقعیه: `/guardian` نشون می‌ده کیه (و اولین بار خودکار قوی‌ترین رو تاج‌گذاری می‌کنه)، `/guardian_challenge` با یه دوئل واقعی عنوان رو می‌گیره، `/guardian_claim` جایزه‌ی روزانه‌ی محافظ فعلیه
- ✅ سیستم rarity کامل (common/rare/epic/legendary/mythic) با ضریب استت
- ✅ **فیوژن** (`/fusion`، قبلاً splice/ترکیب DNA): دو موجود کاربر با هزینه‌ی طلا سوزونده می‌شن (حذف واقعی، نه غیرفعال‌سازی)، فرزند با شانس ارتقای rarity ساخته می‌شه و ۵۰٪ احتمال داره یه تجهیزات از والدین به ارث ببره
- ✅ کلکسیون (`/collection`) و تعویض موجود فعال (`/select`)
- ✅ ماموریت روزانه (`/missions`) — ۸ ماموریت روی feed/train/duel_win/raid_attack/splice/guardian_challenge
- ✅ **انرژی تجدیدشونده**: تغذیه و حمله به رید انرژی مصرف می‌کنن (نه سقف ثابت روزانه) — هر ۱۲ دقیقه یه واحد شارژ می‌شه تا سقف ۲۰، طراحی‌شده تا بازیکن چندبار در روز سر بزنه به‌جای یک‌بار همه‌چی رو خالی کردن
- ✅ **پاداش ورود متوالی (streak)**: هر روز اولین /start پاداش می‌ده، هرچی streak بیشتر پاداش بیشتر (سقف‌دار)، و اگه یه روز جا بمونه ریست می‌شه
- ✅ تبادل سکه/DNA/**مالکیت موجود** بین بازیکن‌ها (`/give coins`, `/give dna`, `/give creature`) و رویداد جهش محیطی یک‌بار-در-روز در هر گروه (`/mutation_event`)
- ✅ `/help` — لیست کامل دستورات، هم توی پیوی هم گروه
- ✅ **دیتابیس با Django ORM** (قبلاً SQLAlchemy بود): مدل‌ها توی `bio_lab/models.py`، migration واقعی داره، و پنل ادمین همه‌چی رو رایگان نشون می‌ده. دلیل سوییچ: نیاز آینده به یه سایت آمار + آشنایی قبلی با Django.
- ✅ متن‌های بات یک‌دست‌سازی شدن: هدر/جداکننده/ایموجی ثابت برای هر مفهوم، نوار پیشرفت متنی (`constants.render_bar`) برای XP/انرژی/HP باس
- ✅ **پیوی کامل و مستقل از گروه**: شکار انفرادی (`/hunt`، هیولای وحشی هم‌سطح بازیکن، بدون نیاز به گروه)، اتحاد بین بازیکن‌ها (`/alliance_create`, `/alliance_join`, `/alliance_leave`, `/alliance_info`, `/alliance_top` — مستقل از گروه تلگرام)، رتبه‌بندی سراسری (`/rank`)، و منوی دکمه‌ای (`/menu`) که همه‌ی این بخش‌ها رو یه‌جا در دسترس می‌ذاره
- ✅ **پیام خوش‌آمد گروه**: وقتی بات به یه گروه اضافه می‌شه، خودکار خودش رو معرفی می‌کنه، دستورات اصلی رو نشون می‌ده و ادمین کامل درخواست می‌کنه
- ✅ **ایموجی پرمیوم قابل‌تنظیم برای سازنده، برای تقریباً کل بات**: `/set_emoji` یه جریان دومرحله‌ای دکمه‌ایه (دسته → کلید → فقط خودِ ایموجی پرمیوم رو بفرست)؛ میان‌بر تک‌پیامی هم هست (`/set_emoji coin` + ایموجی). بات entity پیام رو می‌خونه و `custom_emoji_id` رو ذخیره می‌کنه؛ بعدش همونجا با `<tg-emoji>` رندر می‌شه. `/preview_emoji` یه کارت نمونه + لیست همه‌ی ۴۴ کلید به تفکیک دسته نشون می‌ده تا ببینی نتیجه چی می‌شه («گزینه‌ی تست»). `/list_emoji` و `/clear_emoji` هم هست. فقط `config.OWNER_TELEGRAM_ID` بهش دسترسی داره.
  - **محدودیت مهم:** پیام‌های `GameError`/`RaidError` (که با `query.answer(..., show_alert=True)` یا بدون `parse_mode` فرستاده می‌شن) عمداً از `get_emoji()` استفاده نمی‌کنن — چون بعضی‌هاشون متن آزاد کاربر (مثل `first_name`) رو بدون escape توش دارن و فعال کردن HTML روش می‌تونه ارسال پیام رو با خطای «can't parse entities» بترکونه.
  - **تصحیح یه اشتباه قدیمی توی همین فایل:** قبلاً اینجا نوشته شده بود «روی دکمه‌ها اصلاً نمی‌شه ایموجی پرمیوم گذاشت». **این غلط بود.** `InlineKeyboardButton` دو فیلد داره که دقیقاً همین کار رو می‌کنن: `style` (رنگ) و `icon_custom_emoji_id` (ایموجی پرمیوم قبل از متن). `<tg-emoji>` واقعاً روی دکمه کار نمی‌کنه — ولی اون فقط *یکی* از دو مکانیزمه، و فیلد اختصاصی دکمه راه درستشه. سیستم کامل توی `bot/buttons.py` و `game/button_emoji.py` پایین‌تر توضیح داده شده.
  - **نکته‌ی ظریف:** `HELP_TEXT`/`WELCOME_TEXT` باید تابع باشن نه ثابت ماژول — اگه یه‌بار موقع import ساخته بشن، قبل از `refresh_cache()` توی `main.py` محاسبه می‌شن و همیشه پیش‌فرض نشون می‌دن حتی بعد از `/set_emoji`. هر دو الان به همین دلیل تابعن (`_build_help_text()`, `_build_welcome_text()`)، نه رشته‌ی ثابت.
- ✅ **ابزارهای مدیریتی سازنده**: `/admin` (داشبورد سریع تو خودِ تلگرام + دکمه‌ی لینک به پنل کامل Django؛ همین دکمه زیر `/me`/`/start` هم برای owner اضافه می‌شه)، `/report` (برترین بازیکن‌ها/موجودات + **تشخیص فعالیت مشکوک**: اگه شمارش روزانه‌ی یه اکشن انرژی‌محور از سقف نظری ممکن با انرژی عادی رد بشه پرچم می‌خوره)، `/broadcast متن` (پیام همگانی به همه‌ی کاربرهای ثبت‌شده، با تأخیر بین پیام‌ها برای رعایت محدودیت نرخ تلگرام)
- ✅ **مدیریت کامل کاربر از داخل بات** (نه فقط از سایت): `/user_info` (پروفایل کامل هر بازیکن با آیدی/یوزرنیم)، `/grant` و `/deduct` (اعطا/کسر دستی سکه یا DNA)، `/ban` و `/unban` (مسدودسازی — با `bot/middleware.py` روی کل بات اجرا می‌شه، نه فقط یه پیام هشدار)، `/delete_creature` (حذف واقعی یه موجود، پشت تأیید دکمه‌ای چون برگشت‌ناپذیره و لاگ‌های رید/نبرد مرتبط رو هم cascade می‌کنه)
- ✅ `/profile` (عمومی): آمار کل بازیکن در طول کل بازی — نه فقط امروز — شامل تعداد دوئل‌های برده، شکارهای انجام‌شده، کل دمیج به رید باس‌ها، و streak
- ✅ **سیستم اقتصادی کامل** (طبق طراحی اقتصاد اقتباس‌شده از spec کاربر):
  - **طلا** واحد پول اصلیه (قبلاً «سکه» نمایش داده می‌شد؛ فیلد مدل هنوز `User.coins`ـه، فقط برچسب نمایشی عوض شده — `GIVE_RESOURCE_LABELS`/`emoji.py`)
  - **تجهیزات** (`/inventory`, `/equip`, `/unequip`, `/upgrade_item`): ۴ اسلات (سلاح/زره/طلسم/غلاف)، بونوس‌ها با rarity و سطح (+۱ تا +۱۰) اسکیل می‌شن، ارتقا با مصرف یه نمونه‌ی هم‌نوع + طلا
  - **باکس ژنتیکی / Bio-Crate** (`/biocrate`): با طلا خریداری می‌شه، rarity با جدول احتمال مستقل (common ۷۰٪ … mythic ۰.۵٪) قرعه‌کشی می‌شه، ۵۰٪ موجود یا تجهیزات می‌ده
  - **دوئل با شرط‌بندی طلا**: `/duel 50` (ریپلای روی پیام حریف) یه پیشنهاد با دکمه‌ی قبول/رد می‌فرسته؛ فقط طرف مقابل می‌تونه قبول کنه؛ برد یعنی کل شرط طرف مقابل رو می‌بری (صفر-جمع، بدون تورم اقتصاد). `/duel` بدون عدد همون دوئل رایگان قبلیه.
  - **غارت خزانه‌ی اتحاد (Heist)**: `/alliance_deposit` طلا به خزانه‌ی اتحاد خودت واریز می‌کنه؛ `/heist اسم‌اتحاد` به خزانه‌ی یه اتحاد **دیگه** شبیخون می‌زنه — قوی‌ترین موجود فعال اون اتحاد دفاع می‌کنه (یا شبیخون خودکار موفق می‌شه اگه هیچ‌کس نگهبانی نده)، با کول‌داون روی اتحاد مدافع و سقف تلاش روزانه برای مهاجم
  - جزئیات کامل معماری/باگ‌های پیدا و رفع‌شده‌ی این فاز در بخش‌های `game/equipment.py`, `game/lootbox.py`, `game/fusion.py`, `game/alliance.py`, و «قانون تک موجود فعال» بالاتر
- ✅ **جوین اجباری قابل‌تنظیم از پنل ادمین**: چند کانال هم‌زمان (مدت‌دار یا نامحدود)، هرکدوم با جایزه‌ی اختیاری (طلا/DNA) که فقط یه‌بار به هر کاربر تعلق می‌گیره. افزودن کانال با **فوروارد کردن یه پیام از خودش** انجام می‌شه (نه تایپ کردن آیدی)؛ روی کل بات (پیوی و گروه) اعمال می‌شه. جزئیات کامل توی `game/force_join.py` و `bot/middleware.py` بالاتر.
- ✅ **تجربه‌ی پیوی کاملاً دکمه‌ای («دیزاین شیشه‌ای»)**: کلکسیون/انتخاب موجود، فیوژن (انتخاب دو والد + تأیید)، تجهیزات (equip/unequip/upgrade)، اتحاد (ساخت/پیوستن/واریز/خروج)، شبیخون (انتخاب اتحاد هدف) — همه با دکمه‌های اینلاین، بدون نیاز به تایپ کامند با آرگومان. پنل ادمین هم همینطور (مدیریت کاربر/حذف موجود/پیام همگانی همه با دکمه + یه پرامپت متنی کوتاه). فرمت متنی کامل تلگرام (`<b>`, `<tg-spoiler>`, `<blockquote>`) روی پیام‌های مهم استفاده می‌شه — اسپویلر برای لحظه‌ی «باز کردن» (باکس ژنتیکی، نتیجه‌ی فیوژن، نتیجه‌ی شبیخون)، بلاک‌کوت برای هشدارها/قوانین. دکمه‌های مهم رنگ‌بندی معنایی دارن: 🟢 مثبت/ساخت، 🔴 مخرب/لغو، 🟠 کسر، 🔵 عضویت/پیوستن، ✅/❌ برای تأیید دوتایی. دستورات گروهی (duel/give/…) عمداً کامند موندن چون هدف‌گیری‌شون با ریپلای‌کردنه — جزئیات توی «قانون یه MessageHandler متنی» بالاتر.
- ✅ **الماس، ساختمون‌های idle، گردونه‌ی شانس، جعبه‌های الماسی، و ستاره‌ی پرستیژ**:
  - **الماس** (`User.diamonds`) یه ارز جدا از طلا/DNAـه — منبع اصلیش جمع‌کننده‌ی الماس (خیلی کمیاب، عمداً)، گردونه‌ی روزانه، و جوایز فعالیت نیست (فقط طلا/DNA/کارت سرعت از اونا میاد). مصرفش فقط جعبه‌های الماسیه.
  - **ساختمون‌ها** (`/buildings`): دو نوع (جمع‌کننده‌ی طلا، جمع‌کننده‌ی الماس)، تولید lazy مثل انرژی، **فقط یه کارگر همزمان** (اجرا شده در سطح دیتابیس با `OneToOneField`)، ارتقا با طلا و زمان (سقف ۱۰ سطح)، و کارت‌های سرعت (۱د/۵د/۳۰د/۱س/۱۲س/۲۴س) که زمان باقی‌مونده رو کم می‌کنن یا کلاً تمومش می‌کنن.
  - **گردونه‌ی شانس روزانه** (`/wheel`): یه چرخش رایگان در روز، جدول جایزه‌ی وزن‌دار شامل طلا/DNA/الماس/کارت سرعت.
  - **جعبه‌های الماسی** (`/diamondbox`): ۴ سطح (برنزی تا افسانه‌ای) با هزینه و جدول احتمال کاملاً مستقل از هم؛ برخلاف Bio-Crate همیشه موجود می‌دن، نه تجهیزات؛ درصد دقیق هر رده قبل از خرید نشون داده می‌شه.
  - **ستاره‌ی پرستیژ** (`Creature.star_level`, پیش‌فرض ۱، سقف `STAR_MAX=5`): هیچ‌وقت مستقیم توسط بازیکن قابل تنظیم نیست — فقط از فیوژن میاد (`max(دو والد) + 1`) و یه بونوس کوچیک استت روی خودِ موجود می‌ده، جدا از rarity/رنگ.
  - جزئیات کامل معماری توی `game/buildings.py`, `game/wheel.py`, `game/lootbox.py`, `game/fusion.py` بالاتر.
- ✅ **پالایش UX (فاز اسم آزمایشگاه/شکار/نبرد)**:
  - **اسم آزمایشگاه**: اولین `/start` اسم می‌پرسه و بعدش توی پیام خوش‌آمد استفاده می‌شه.
  - **جایزه‌ی جوین اجباری با الماس** + نمایش جایزه‌ی هر کانال توی همون پیام «باید عضو بشی»، و گزارش دقیق جایزه‌ی گرفته‌شده بعد از تأیید.
  - **شارژ چندمنبعی ادمین**: دکمه‌ی «⚡ شارژ کامل» توی پنل مدیریت کاربر + دستور `/charge آیدی طلا DNA الماس` — هر سه با هم، یا با گذاشتن صفر فقط بعضی‌هاشون.
  - **کارت موجود ساده‌سازی شد** و بخش ارتقا جدا شد (بالاتر، `bot/handlers/private.py`).
  - **شکار انفرادی حالا انتخاب حریف داره**: سه حریف با قدرت و غنیمت مشخص، انرژی فقط موقع انتخاب خرج می‌شه (بالاتر، `game/hunt.py`).
  - **لاگ نبرد فشرده شد**: یه خط به‌ازای هر حمله (بالاتر، `game/combat.py`).
- ✅ **آرنای کاپ، آهنگری، و ستاره‌ی سخت‌گیرانه**:
  - **آرنا** (`/arena`): مچ‌میکینگ با کاپ، غارت ۱۰٪ طلای حریف، سپر ۸ ساعته بعد از حمله‌خوردن که با حمله‌کردن خودت می‌پره، حریف فیک وقتی بازیکن واقعی نیست، و سقف نرم کاپ بر اساس قدرت واقعی. جزئیات کامل توی `game/arena.py` بالاتر.
  - **آهنگری** (`/blacksmith`): ارتقای تجهیزات با طلا و ریسک شکست — جدا از مسیر بی‌ریسک «مصرف نمونه‌ی تکراری». جزئیات توی `game/blacksmith.py` بالاتر.
  - **ستاره فقط با جفت هم‌گونه/هم‌ستاره**: ۱★ تا ۵★، و XP والدین جمع می‌شه (بالاتر، `game/fusion.py`).
  - **اسم‌های فارسی خوانا** برای هیولاها (`SPECIES_NAMES`) — اسم حالا **کلید هویت فیوژن** هم هست، نه فقط تزئین.
  - **صفحه‌ی ارتقا انتخابگر داره**: هیولاها به ترتیب قدرت نزولی لیست می‌شن و هر اکشن (`lab:<action>:<creature_id>`) روی همون هیولای انتخابی اجرا می‌شه — عمداً موجود فعال رو عوض نمی‌کنه.
  - **شکار تک‌حریفی**: هر بار یک حریف + دکمه‌ی «بعدی» رایگان، انرژی فقط موقع حمله.
- ✅ **فصل هفتگی کاپ**: جدول آرنا هر هفته ریست می‌شه؛ هرچی رتبه‌ت بالاتر باشه از کاپ بالاتری شروع می‌کنی. جدول همون لحظه نشون می‌ده هر رتبه به چه کاپی ریست می‌شه، و نتایج هفته‌ی قبل هم قابل دیدنه. جزئیات توی `game/season.py` بالاتر.
- ✅ **کارت سرعت واقعاً به بازیکن می‌رسه**: بسته‌ی شروع (`STARTING_SPEEDUP_CARDS`) که برای رد کردن کند‌ترین ارتقاهای اول کافیه، ۹ ماموریت روزانه که کارت می‌دن، و جایزه‌ی شانسی از فعالیت‌های عادی. **ارتقا با الماس هم تموم می‌شه** (`diamond_finish_cost`) و قیمتش از زمان **باقی‌مونده** حساب می‌شه، پس هرچی بیشتر صبر کنی ارزون‌تره.
- ✅ **دکمه‌های رنگی واقعی + ایموجی پرمیوم روی دکمه**: کل بات از `bot/buttons.py` دکمه می‌سازه — رنگ معنایی (آبی=اکشن اصلی، سبز=تأیید/ساخت/جایزه، قرمز=مخرب/غارت) و ایموجی پرمیوم روی خودِ دکمه. سازنده از پنل ادمین → «🎛 ایموجی دکمه‌ها» برای هر کدوم از ۳۴ دکمه‌ی مهم جدا ایموجی تنظیم می‌کنه (جدا از «🎨 ایموجی متن‌ها»). جزئیات توی `bot/buttons.py` و `game/button_emoji.py` بالاتر.
- ✅ **پیشرفت ساختمون‌محور** (`/buildings`): ۶ ساختمون که همه سقف ۵ دارن و **هیچ‌کدوم نمی‌تونن از سطح «🏛 تالار مِهر» (ساختمان اصلی) جلو بزنن**. آهنگری و تالار ادغام باید **ساخته بشن** تا ارتقای تجهیزات و فیوژن اصلاً ممکن بشه؛ سقف سطح تجهیزات = سطح آهنگری × ۵، و سقف ستاره‌ی هیولاها = سطح تالار مِهر. هدیه‌ی شروع هم به ۵۰۰۰ طلا + ۱۰۰ DNA + ۱۰۰ الماس رسید تا بازیکن تازه از روز اول بتونه بسازه و جعبه باز کنه (بازیکن‌های قدیمی با migration `0013` تا همین کف بالا آورده شدن). جزئیات کامل توی `game/buildings.py`, `game/blacksmith.py`, `game/fusion.py` بالاتر.

- ✅ **دسترسی کامل دکمه‌ای به انتخاب موجود و ترکیب**: صفحه‌ی مستقل «🧪 ترکیب هیولا» (`fusion_panel`) که روی `ready_pairs()` سواره و مستقیم از کارت `/start` و از `/menu` یه دکمه فاصله داره — یعنی بازیکن دیگه لازم نیست از مسیر کلکسیون → انتخاب موجود → امید به وجود شریک بره. لیست کلکسیون هم کنار هر موجود یه دکمه‌ی «فعال کن» (`coll_select:`) گرفت تا انتخاب موجود فعال یک‌ضرب باشه، نه دو صفحه اون‌ورتر. متن تأیید فیوژن هم صریحاً می‌گه این ترکیب **۱۰۰٪ موفق می‌شه**. جزئیات باگی که این فاز رفع کرد توی `game/fusion.py` بالاتر.

- ✅ **پالت رنگ معنایی + پنل تحت وب + لودآوت + پشتیبان‌گیری + Docker/Postgres**:
  - **رنگ دکمه‌ها از «رنگ» به «نقش» تبدیل شد** (`game/button_style.py`). قبلاً هر call site مستقیم `KeyboardButtonStyle` می‌داد و منوها تقریباً همه آبی بودن؛ حالا call site نقش می‌ده و پالت یه جا تعریف/تنظیم می‌شه. ناوبری بی‌رنگ شد و رنگ فقط روی کنش‌ها موند.
  - **`panel/` — پنل تحت وب اپراتور**، جدا از ادمین جنگو: داشبورد، پالت رنگ (با پیش‌نمایش زنده)، ایموجی دکمه‌ها، ایموجی متن‌ها، لودآوت‌ها، بازیکنان، جوین اجباری، پشتیبان‌گیری. RTL و موبایل‌پسند.
  - **لودآوت‌های ظاهر** (`game/theme.py` + `ThemeLoadout`): چند تم بساز، با یه کلیک سوییچ کن، خروجی/ورودی JSON بگیر. فقط ظاهر — هیچ داده‌ی بازی داخلش نیست.
  - **پشتیبان‌گیری و بازیابی کامل** (`game/backup.py`): آرشیو `.json.gz` مستقل از موتور دیتابیس، با تأیید تایپی برای بازیابی. چون `flush` سشن‌ها رو هم پاک می‌کنه، ویو بعد از بازیابی اپراتور رو دوباره لاگین می‌کنه (جزئیاتش پایین‌تر).
  - **Docker + Postgres**: یه ایمیج مشترک با دو نقش (`web`/`bot`) که با `command` انتخاب می‌شه، `docker-compose.yml` با healthcheck و ولوم برای پشتیبان‌ها، و `DEPLOY.md`. جزئیات کامل توی `game/button_style.py`, `game/theme.py`, `game/backup.py`, `panel/views.py` بالاتر.

**تعادل اقتصادی (مهم برای تغییرات بعدی):** سه محور درآمد عمداً به یه محور پیشرفت مشترک گره خوردن — **سطح هیولا**:
- شکار: `hunt_coin_range(level, tier)` با سطح بالا می‌ره. قبلاً ثابت ۱۵–۳۵ طلا بود و چون هزینه‌ی ساختمون/آهنگری با سطح بالا می‌رفت، شکار خیلی زود بی‌ارزش می‌شد — همون چیزی که اقتصاد رو خراب کرده بود.
- آرنا: غارت ۱۰٪ طلای مدافعه، ولی با `arena_loot_cap(attacker_level)` سقف می‌خوره. سقف روی **سطح مهاجمه نه ثروت مدافع** — وگرنه یه مچ شانسی با یه بازیکن پول‌دار کل اقتصاد اولیه رو دور می‌زد.
- حریف‌های فیک هم با `arena_fake_loot_range(level)` بالا می‌رن تا کهنه نشن.
قاعده‌ی کلی: آرنا باید یه‌کم بهتر از شکار بده، نه جک‌پات (تست ۲۸ این نسبت رو صریحاً چک می‌کنه).

همه‌ی این‌ها با اسکریپت‌های smoke test (خارج از ریپو، در scratchpad) به‌صورت end-to-end تست شدن — شامل تست صریح regen انرژی با شبیه‌سازی گذر زمان (`energy_updated_at` رو دستی عقب می‌بریم)، streak با شبیه‌سازی روزهای مختلف، و انتقال/حذف خودکار رهبری اتحاد. حین ساخت MVP اولیه یه باگ بحرانی (مقایسه‌ی datetime timezone-aware/naive که بعد از reload از SQLite کرش می‌کرد) و یه باگ HTML-parsing (تگ خام `<id>` توی پیام با `parse_mode="HTML"` که تلگرام ردش می‌کرد) پیدا و رفع شدن. حین سوییچ به Django، یه باگ سوم هم پیدا شد: `lab_action_callback` توی `private.py` در برخی مسیرها `query.answer()` رو دوبار صدا می‌زد. تست واقعی روی تلگرام انجام شده (بات @HeroGameZbot زنده‌ست).

**باگ‌های پیدا و رفع‌شده در فاز اقتصاد/تجهیزات:** (۱) `display_name()` توی `bio_lab/repository.py` یوزرنیم/نام کاربر رو بدون escape توی پیام‌های `parse_mode="HTML"` می‌ذاشت — یه یوزرنیم حاوی `<`/`&` می‌تونست رندر پیام رو بترکونه؛ با `html.escape()` رفع شد (یه‌جا، چون همه‌جا از همین تابع صدا زده می‌شه). (۲) `game.equipment.get_equipped_items()` با یه `Creature` ذخیره‌نشده (مثل هیولای وحشی `hunt.py`) کرش می‌کرد چون Django فیلتر روی instance بدون pk رو قبول نمی‌کنه — این باگ واقعاً روی بات زنده حین `/hunt` رخ داد و با یه چک `creature.pk is None` رفع شد. (۳) `lootbox._roll_creature` و `fusion.fuse` قانون «تک موجود فعال» رو نقض می‌کردن (بالاتر توضیح داده شده) — روی دیتابیس واقعی هم یه کاربر واقعاً دچار این حالت شده بود و دستی رفع شد.

**باگ فاز پنل/پشتیبان‌گیری:** بازیابی، `flush` می‌زنه که جدول `django_session` رو هم خالی می‌کنه — یعنی اپراتور با فشار دادن دکمه‌ی بازیابی، خودش رو از همون صفحه بیرون می‌انداخت. بدتر: توی Django 6.1 تابع `login()` وقتی سشن فعلی مال همون کاربره **کلید سشن رو عوض نمی‌کنه**، پس میدل‌ور سشن آخر ریکوئست یه UPDATE روی ردیفی می‌زد که دیگه وجود نداشت و کل پاسخ ۴۰۰ می‌شد — یعنی بازیابیِ موفق، از بیرون شبیه کرش دیده می‌شد. راه‌حل: `request.session.flush()` **بی‌قید‌وشرط** قبل از `login()` توی `panel/views.py::_relogin_after_restore` تا کلید مرده دور ریخته بشه و یه ردیف تازه INSERT بشه؛ همین برای حالتی که اصلاً حساب بازیابی‌شده‌ای وجود نداره هم لازمه، چون ریکوئست به‌هرحال باید تمیز تموم بشه.

**فاز جوین اجباری/UI دکمه‌ای:** یه تله‌ی معماری واقعی موقع طراحی پیدا و ازش دوری شد: تلاش اولیه برای اینکه هر فلوی «منتظر پیام بعدی» (owner و player) یه `MessageHandler` جدا داشته باشه، چون فیلترهاشون همپوشانی داشتن (owner هم یه بازیکنه) توی یه `group` عددی یکی می‌شدن و طبق رفتار PTB («فقط اولین match هر group اجرا می‌شه») هندلر دوم بی‌صدا هیچ‌وقت اجرا نمی‌شد. راه‌حل نهایی همون «قانون یه MessageHandler متنی» بالاتره.

بعد از دیپلوی، یه باگ واقعی هم روی بات زنده رخ داد: زدن دوباره‌ی دکمه‌ی «♾ نامحدود کن» روی یه کانالی که از قبل نامحدود بود (یا هر دکمه‌ی دیگه‌ای که قراره همون محتوای فعلی رو دوباره رندر کنه) باعث می‌شد تلگرام با `BadRequest: Message is not modified` رد کنه، چون متن+کیبورد جدید دقیقاً با چیزی که همین الان روی صفحه‌ست یکی بود — و چون هیچ error handler‌ای رجیستر نشده بود، این exception فقط لاگ می‌شد و کاربر هیچ فیدبکی نمی‌دید (انگار دکمه کار نکرده). رفع شد با `bot.utils.safe_edit_message_text()` — یه wrapper دور `query.edit_message_text()` که دقیقاً همین یه ارور رو (بر اساس متن پیامش) قورت می‌ده و بقیه‌ی `BadRequest`ها رو عادی propagate می‌کنه. چون این کلاس باگ می‌تونست روی **هر کدوم** از ده‌ها جای `edit_message_text` توی این فاز رخ بده (هر جا یه اکشن idempotent باشه یا کاربر دوبار پشت‌سرهم روی یه دکمه بزنه)، به‌جای پچ موضعی، همه‌ی صداهای `query.edit_message_text(` توی کل `bot/handlers/` و `bot/middleware.py` با این wrapper جایگزین شدن — از این به بعد **هر `edit_message_text` جدید باید از `safe_edit_message_text` استفاده کنه، نه مستقیم از متد خودِ `query`**.

**نکته‌ی PyCharm:** فایل‌های `.run/*.run.xml` باید حتماً `<module name="TelGame" />` داشته باشن — بدون این تگ، PyCharm 2025.2 موقع اجرا تو یه thread پس‌زمینه exception می‌ندازه (`ModuleRootManager.getInstance` با module=null) که هیچ‌وقت به کنسول Run نمی‌رسه، پس به‌جای خطا فقط چرخ‌وفلک بی‌پایان می‌بینی. اگه بعداً Run Configuration جدیدی اضافه کردی، این تگ رو یادت نره.

## فاز بعد (برنامه‌ریزی‌شده، هنوز پیاده نشده)

- جنگ قلمرو بین گروه‌ها (حمله‌ی یه گروه به قلمرو گروه دیگه؛ فعلاً guardian فقط داخل یه گروهه، بین دو تا گروه تلگرام مجزا هنوز مکانیزم نداریم — Heist این خلأ رو در سطح اتحاد پر کرد، نه در سطح گروه تلگرام)
- بازار عمومی (لیست کردن موجود/تجهیزات برای فروش با قیمت‌گذاری، نه فقط هدیه‌ی مستقیم دو نفره)
- ماموریت هفتگی، رویدادهای فصلی
- سایت آمار عمومی (خارج از پنل ادمین) — حالا که دیتابیس Django ـه، فقط باید `views.py`/`urls.py`/تمپلیت اضافه کرد، مدل‌ها همینایی هستن که بات هم استفاده می‌کنه
- افکت بصری/دمیج-به-کل-تیم برای rarity اساطیری (mythic) توی لوت‌باکس — الان mythic فقط ضریب استت بالاتر داره، مکانیزم ویژه‌ی جداگانه‌ای که در طراحی اقتصاد اشاره شده بود هنوز پیاده نشده
- ساختمون‌های بیشتر — فعلاً فقط ۲ نوع (جمع‌کننده‌ی طلا/الماس) پیاده شده؛ افزودن نوع جدید فقط یه ورودی به `constants.BUILDING_TYPES`/`BUILDING_LABELS`/`BUILDING_PRODUCTION` می‌خواد، بقیه‌ی `game/buildings.py` عمومیه و نیازی به تغییر نداره
